# * Create the ui and server template for the custom module
# * Create custom module to show the content of selected dataset
# * Create selectInput for the datasets, set the choices to NULL and explain
# * Notice the data argument in the server argument
# * use updateSelectInput to update the datasets input, set choices to datanames(data())
# * Display the dataset in data table format
